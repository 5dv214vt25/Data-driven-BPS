# Pytest configuration: quiet output, test discovery, and src import support

[pytest]
minversion = 6.0
addopts = -ra -q
testpaths = 
    tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# This ensures src is discoverable for imports
pythonpath = 
    src