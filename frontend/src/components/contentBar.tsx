import { useEffect } from "react";

/**
 * Handles the content topbar
 * 
 * @component
 */

function ContentBar() {
  useEffect(() => {
  }, []);

  return (
    <div className="contentbar">
      Settings!
    </div>
  );
}

export default ContentBar;